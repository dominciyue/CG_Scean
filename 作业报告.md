# 计算机图形学作业报告
## 基于OpenGL的中式室内场景交互系统

---

## 一、台灯光照系统

### 1.1 光照模型选择与实现

本项目采用**Blinn-Phong光照模型**实现台灯的光照效果。该模型将光照分解为三个分量：

$$I_{total} = I_a + I_d + I_s$$

其中：
- $I_a$ - 环境光分量 (Ambient)：提供基础照明，避免完全黑暗
- $I_d$ - 漫反射分量 (Diffuse)：模拟粗糙表面的漫反射
- $I_s$ - 镜面反射分量 (Specular)：模拟光滑表面的镜面高光

#### 为什么选择Blinn-Phong而非标准Phong？

标准Phong模型使用反射向量 $\vec{R}$ 与视线向量 $\vec{V}$ 的点积计算镜面高光：

$$I_s^{Phong} = k_s \cdot (\vec{R} \cdot \vec{V})^{n}$$

这种方法存在两个问题：
1. **性能问题**：需要额外计算反射向量 $\vec{R} = 2(\vec{N} \cdot \vec{L})\vec{N} - \vec{L}$
2. **边缘截断**：当视角与反射方向夹角超过90°时，点积为负，产生视觉上的突变

Blinn-Phong引入**半程向量 (Halfway Vector)** $\vec{H}$：

$$\vec{H} = \frac{\vec{L} + \vec{V}}{|\vec{L} + \vec{V}|}$$

镜面分量简化为：

$$I_s^{Blinn} = k_s \cdot \max(\vec{N} \cdot \vec{H}, 0)^{n}$$

这种方法的优势：
- 无需计算反射向量，减少一次向量运算
- 半程向量与法线的夹角范围更合理，避免截断
- 在相同高光指数下，视觉效果更柔和

### 1.2 点光源衰减函数

台灯作为点光源，光照强度随距离衰减。采用**二次衰减函数**：

$$F_{att} = \frac{1}{K_c + K_l \cdot d + K_q \cdot d^2}$$

其中：
- $K_c$ - 常数项，保证分母不为零
- $K_l$ - 线性衰减系数
- $K_q$ - 二次衰减系数
- $d$ - 片元到光源的距离

#### 衰减系数的调参过程

最初使用了标准值（$K_l=0.09, K_q=0.032$），但发现光照范围过大，整个房间都被照亮，不符合台灯局部照明的特点。

经过多次调试，最终选择：
- $K_c = 1.0$
- $K_l = 0.7$：增大线性衰减，使中等距离的光照快速减弱
- $K_q = 1.8$：显著增大二次衰减，使远距离快速衰减至黑暗

这样台灯的有效照明半径约为1.5个单位长度，符合桌面台灯的真实效果。

### 1.3 台灯开关交互实现

#### 点击检测的优化过程

**问题1：点击台灯底座也能触发开关**

最初实现使用了整个台灯模型的包围球进行射线相交检测，导致点击台灯底座也会触发开关，这不符合真实交互逻辑（开关应该在灯罩上）。

**解决方案：精确点击区域**

修改射线检测逻辑，仅对台灯的**圆柱形灯罩部分**进行检测：

1. 计算射线与圆柱体的相交
2. 限定Y轴范围在灯罩高度内
3. 检查XZ平面上的距离是否在灯罩半径内

这样只有点击灯罩才会触发开关，提升了交互的真实感。

#### 开关状态的视觉反馈

台灯的亮灭通过片元着色器中的条件分支实现：

```glsl
if (lampOn) {
    // 灯罩发光
    FragColor = vec4(emissiveColor, 1.0);
    // 计算台灯光源贡献
    lightContribution += calculateLampLight(...);
} else {
    // 灯罩使用基础材质色
    FragColor = vec4(baseColor, 1.0);
}
```

### 1.4 灯罩发光效果

灯罩的发光通过**自发光项 (Emissive)** 实现：

$$I_{shade} = I_{emissive} + I_{ambient}$$

自发光的特点：
- 不受光源方向影响
- 呈现均匀的内部发光效果
- 颜色选择暖黄色 $(1.0, 0.9, 0.7)$，模拟白炽灯光

同时提高灯罩颜色的饱和度和亮度，使其在暗环境中显得"发光"。

### 1.5 光晕效果实现

光晕效果采用**加性混合 (Additive Blending)** 的半透明渲染：

$$C_{final} = C_{src} \cdot \alpha_{src} + C_{dst}$$

实现步骤：
1. 在台灯位置渲染一个放大的半透明球体
2. 启用 `glBlendFunc(GL_SRC_ALPHA, GL_ONE)`
3. 颜色使用柔和的黄色，透明度设为0.3-0.5
4. 可叠加多层不同大小的光晕球体，形成渐变效果

这种方式虽然不如屏幕空间后处理的Bloom效果真实，但实现简单且性能友好。

---

## 二、地板纹理系统

### 2.1 纹理坐标映射

地板采用中式地砖纹理，为了使纹理平铺时保持正确的比例，需要根据地板的实际世界坐标计算UV坐标：

$$u = \frac{x_{world}}{tileSize} \mod 1, \quad v = \frac{z_{world}}{tileSize} \mod 1$$

其中 $tileSize$ 是单个地砖的世界空间尺寸。

### 2.2 纹理过滤与Mipmap

使用**三线性过滤 (Trilinear Filtering)**：
```cpp
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MIN_FILTER, GL_LINEAR_MIPMAP_LINEAR);
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_MAG_FILTER, GL_LINEAR);
```

Mipmap的作用：
- 当地板远离相机时，使用低分辨率纹理
- 避免在斜视角度产生摩尔纹和闪烁
- 提升渲染性能（减少纹理采样次数）

### 2.3 纹理环绕模式

设置 `GL_REPEAT` 使纹理无缝平铺：
```cpp
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_S, GL_REPEAT);
glTexParameteri(GL_TEXTURE_2D, GL_TEXTURE_WRAP_T, GL_REPEAT);
```

---

## 三、物体交互系统

### 3.1 射线拾取原理

射线拾取是3D场景交互的核心技术，将2D屏幕坐标转换为3D世界空间的射线。

#### 坐标系统转换流程

**第一步：屏幕坐标 → NDC (Normalized Device Coordinates)**

屏幕坐标原点在左上角，Y轴向下；NDC原点在中心，范围[-1, 1]：

$$x_{ndc} = \frac{2 \cdot x_{screen}}{width} - 1$$
$$y_{ndc} = 1 - \frac{2 \cdot y_{screen}}{height}$$

**第二步：NDC → 裁剪空间**

构造裁剪空间坐标，Z=-1表示近平面：

$$\vec{P}_{clip} = (x_{ndc}, y_{ndc}, -1.0, 1.0)$$

**第三步：裁剪空间 → 视图空间**

使用投影矩阵的逆变换：

$$\vec{P}_{view} = P^{-1} \cdot \vec{P}_{clip}$$

然后进行透视除法：

$$\vec{P}_{view} = \frac{\vec{P}_{view}}{w}$$

**第四步：视图空间 → 世界空间**

使用视图矩阵的逆变换：

$$\vec{P}_{world} = V^{-1} \cdot \vec{P}_{view}$$

**第五步：计算射线方向**

$$\vec{D}_{ray} = normalize(\vec{P}_{world} - \vec{Camera}_{pos})$$

### 3.2 射线-球体相交检测

对场景中的物体使用**包围球 (Bounding Sphere)** 进行快速相交测试。

#### 数学推导

设：
- 射线原点：$\vec{O}$
- 射线方向：$\vec{D}$ (单位向量)
- 球心：$\vec{C}$
- 球半径：$r$

射线参数方程：

$$\vec{P}(t) = \vec{O} + t\vec{D}, \quad t \geq 0$$

相交条件：

$$|\vec{P}(t) - \vec{C}|^2 = r^2$$

展开：

$$|\vec{O} + t\vec{D} - \vec{C}|^2 = r^2$$

$$(\vec{O}-\vec{C}) \cdot (\vec{O}-\vec{C}) + 2t\vec{D} \cdot (\vec{O}-\vec{C}) + t^2(\vec{D} \cdot \vec{D}) = r^2$$

整理为标准二次方程 $at^2 + bt + c = 0$：

- $a = \vec{D} \cdot \vec{D} = 1$ (因为$\vec{D}$是单位向量)
- $b = 2\vec{D} \cdot (\vec{O}-\vec{C})$
- $c = |\vec{O}-\vec{C}|^2 - r^2$

判别式：

$$\Delta = b^2 - 4ac$$

- $\Delta < 0$：射线与球体不相交
- $\Delta = 0$：射线与球体相切
- $\Delta > 0$：射线与球体相交于两点

取较小的正根作为交点：

$$t = \frac{-b - \sqrt{\Delta}}{2a}$$

### 3.3 物体选中高亮

#### 悬停高亮效果

当鼠标悬停在物体上时，通过修改片元着色器的输出颜色实现高亮：

$$C_{final} = C_{base} + k_{highlight} \cdot C_{accent}$$

其中 $k_{highlight}$ 通过时间函数产生**呼吸效果**：

$$k_{highlight}(t) = 0.5 + 0.5 \cdot \sin(\omega t)$$

$\omega = 4.0$ 控制呼吸频率。

### 3.4 交互类型的设计思考

在实现过程中遇到了**物理合理性**的问题：

#### 问题：交互方式的选择

最初设计：
- 花瓶：平移
- 书籍：平移
- 卷轴：旋转

**遇到的问题**：卷轴是长条形物体，如果绕中心旋转会出现"悬空旋转"的不合理现象，违反物理直觉。

**解决方案**：重新设计交互类型

| 物体 | 交互类型 | 设计理由 |
|------|----------|----------|
| 花瓶 | 旋转 | 圆柱体绕自身轴线旋转符合直觉，且适合作为机关开关 |
| 书籍 | 平移 | 书籍可推动，平移动画自然 |
| 卷轴 | 机关触发 | 避免不合理的空中旋转，改为触发解谜机关 |

#### 交互方式的优化：点击触发

最初的设计是**拖拽操作**，需要按住鼠标拖动才能旋转或平移物体。

**问题**：操作复杂，不够直观。

**改进**：改为**点击触发自动动画**
- 点击一次：物体自动播放旋转/平移动画
- 再次点击：播放反向动画，恢复原位

这样交互更简单，且动画更流畅。

### 3.5 重置功能的实现

为避免物体移出场景，添加了**重置功能**。

实现思路：
1. 记录物体的初始位置和旋转
2. 添加 `hasAnimated` 标志，跟踪动画状态
3. 第一次点击：正向动画，设置 `hasAnimated = true`
4. 第二次点击：反向动画，恢复到初始状态

核心逻辑：
```
if (hasAnimated) {
    // 播放反向动画
    targetPosition = originalPosition
    isResetting = true
} else {
    // 播放正向动画
    targetPosition = originalPosition + offset
    isResetting = false
}
```

---

## 四、解谜游戏：灵珠机关

### 4.1 机关设计

当玩家点击正确的物体（卷轴）时，触发地板机关：

1. 地板砖翻转
2. 暗格显现
3. 灵珠从暗格中升起
4. 灵珠发光并影响周围物体

### 4.2 地砖翻转动画

#### 遇到的问题：翻转方向错误

**问题1：地砖往下翻，而不是往上**

最初实现时，地砖旋转90°后翻到了地板下方，无法看到暗格。

**原因分析**：
- 旋转中心选择错误
- 旋转角度方向错误

**解决方案**：
1. 将旋转中心设置在地砖的**前边缘**（Z=0）
2. 绕X轴正向旋转+90°，使地砖向后翻起

变换矩阵构造：
$$M_{flip} = T_{pivot} \cdot R_x(+90°) \cdot T_{-pivot}$$

其中 $T_{pivot}$ 是平移到旋转轴的变换。

**问题2：地砖纹理与房间地板不匹配**

地砖翻起后，纹理出现"花纹割裂感"，颜色也有高光显示，与周围地板不一致。

**原因分析**：
- 纹理坐标没有与房间地板对齐
- 光照计算产生了不必要的镜面高光

**解决方案**：
1. 调整地砖的纹理坐标，使其与房间地板纹理的相同位置对齐
2. 降低地砖材质的镜面反射系数

**问题3：地砖位置"凸起"**

地砖初始状态高于地板表面，看起来很突兀。

**解决方案**：
将地砖的Y坐标调整为：
$$Y_{tile} = Y_{floor} - thickness$$

使地砖的上表面与地板齐平。

### 4.3 灵珠升起动画

#### 缓动函数的选择

为使动画更自然，采用**Ease-Out三次函数**：

$$f(t) = 1 - (1-t)^3$$

函数特点：
- $f(0) = 0$，$f(1) = 1$
- 一阶导数：$f'(t) = 3(1-t)^2$
- 二阶导数：$f''(t) = -6(1-t)$

物理意义：
- 初始速度较快
- 逐渐减速
- 符合物体受阻力的运动规律

灵珠位置插值：

$$Y(t) = Y_{start} + (Y_{end} - Y_{start}) \cdot f(t)$$

#### 遇到的问题：升起时地板没有洞

**问题**：灵珠升起时，地板仍然是完整的，没有看到"黑色方形洞"。

**原因**：
- 翻起的地砖仍然遮挡了暗格
- 暗格渲染在地砖之后

**解决方案**：
1. 调整渲染顺序：先渲染暗格（黑洞），再渲染地砖
2. 地砖翻起后，暗格自然显露
3. 在暗格位置渲染一个**黑色立方体**，模拟洞的深度

### 4.4 灵珠光芒特效

#### 核心发光

灵珠使用**自发光材质 + Phong着色器**：

$$C_{orb} = C_{emissive} + C_{specular}$$

颜色选择神秘的蓝青色调：
$$C_{core} = (0.4, 0.8, 1.0)$$

#### 光芒脉动

通过正弦函数产生周期性脉动：

$$I_{glow}(t) = I_{base} + A \cdot \sin(\omega t + \phi)$$

- $I_{base} = 0.8$：基础亮度
- $A = 0.2$：脉动幅度
- $\omega = 3.0$：脉动频率
- $\phi$：相位偏移（用于多光源时错开相位）

#### 射线光芒系统

从灵珠中心向外发射8条射线光芒：

**几何结构**：
每条射线是一个拉长的三角形网格，从灵珠表面延伸到一定距离。

**空间分布**：
射线均匀分布在水平面上：

$$\theta_i = \frac{2\pi i}{N}, \quad i = 0, 1, ..., N-1$$

其中 $N=8$ 是射线数量。

**旋转动画**：
射线随灵珠旋转：

$$\theta_{final} = \theta_{base} + \omega_{spin} \cdot t$$

旋转速度 $\omega_{spin} = 60°/s$。

**射线变换矩阵**：

$$M_{ray} = T_{orb} \cdot R_y(\theta_{final}) \cdot T_{offset} \cdot S_{ray}$$

- $T_{orb}$：平移到灵珠位置
- $R_y(\theta_{final})$：绕Y轴旋转
- $T_{offset}$：从球心偏移到表面
- $S_{ray}$：缩放射线长度和宽度

**脉动效果**：
每条射线有独立的相位偏移：

$$I_i(t) = 0.6 + 0.4 \cdot \sin(\omega t + \phi_i)$$
$$L_i(t) = L_{base} \cdot (0.8 + 0.3 \cdot \sin(1.5\omega t + \phi_i))$$

其中 $\phi_i = \frac{2\pi i}{N}$ 是相位偏移。

### 4.4 灵珠光源对场景的影响

灵珠不仅是视觉特效，还作为**动态点光源**影响周围物体。

#### 光源激活条件

灵珠升起到一定高度（30%）后才开始发光：

```
if (orbRiseProgress > 0.3) {
    orbEmitting = true
}
```

这样形成"灵珠从暗格中浮现，逐渐发光"的视觉效果。

#### 光照计算

在片元着色器中添加灵珠光源项：

$$I_{total} = I_{ambient} + I_{lamp} + I_{orb}$$

其中灵珠光照使用与台灯相同的Blinn-Phong模型，但使用不同的颜色和强度。

---

## 五、诱敌机关：箭矢陷阱系统

### 5.1 机关触发流程

```
点击花瓶 → 花瓶旋转 → 书柜滑动 → 暗格显露 → 点击暗格 → 门板滑开 → 箭矢发射
```

### 5.2 书柜滑动动画

书柜贴墙放置
$$X_{bookcase} = X_{room} + 0.5 \cdot W_{room} - 0.5 \cdot W_{bookcase}$$

沿Z轴（垂直于墙面）滑动：
$$Z_{new} = Z_{old} + \Delta Z$$

#### 平滑动画

使用Ease-Out缓动函数：

$$p(t) = p_{start} + (p_{end} - p_{start}) \cdot (1 - (1-t)^3)$$

### 5.3 墙面暗格与门板动画

#### 暗格位置问题

**问题**：暗格最初位置太靠后（嵌入墙内太深），从房间内看不到。

**解决方案**：
将暗格位置调整到墙表面：
$$X_{compartment} = X_{wall} - 0.01$$

暗格的"黑洞"向墙内延伸，而门板在墙表面。

#### 活板门滑动动画

**设计**：暗格门板像活板门一样上下滑开，从中间分为两部分：
- 上半部分向上滑动
- 下半部分向下滑动

**数学描述**：

上门板的可见高度：
$$H_{upper} = H_{half} - \Delta H(t)$$

下门板的可见高度：
$$H_{lower} = H_{half} - \Delta H(t)$$

其中：
$$\Delta H(t) = H_{half} \cdot easeOut(t)$$

上门板位置：
$$Y_{upper} = Y_{center} + \Delta H(t) + 0.5 \cdot H_{upper}$$

下门板位置：
$$Y_{lower} = Y_{center} - \Delta H(t) - 0.5 \cdot H_{lower}$$

#### 遇到的问题：门板动画错误

**问题**：门板分成四个部分滑动，而不是两个部分。

**原因**：渲染逻辑错误，每个门板被重复渲染。

**解决方案**：
修改渲染逻辑，明确只渲染两个门板，并在滑动进度为1时停止渲染门板。

### 5.4 箭矢粒子系统

#### 粒子结构设计

每个箭矢粒子包含：

```
struct Arrow {
    position       // 位置向量 (3D)
    velocity       // 速度向量 (3D)
    rotation       // 朝向 (四元数或欧拉角)
    lifetime       // 剩余生命 (秒)
    collided       // 是否已碰撞
    collisionPoint // 碰撞点位置
    active         // 是否激活
}
```

#### 多发射器设计

系统包含两个发射器：

**发射器1：墙面暗格**
- 位置：墙面暗格的黑洞内
- 方向：向房间内发射（-X方向）
- 参数：
  - 初速度：5.0 单位/秒
  - 散布角度：±15°
  - 发射间隔：0.1秒
  - 每次发射：10支箭

**发射器2：窗户**
- 位置：窗户外侧
- 方向：向房间内发射
- 参数：
  - 初速度：6.0 单位/秒
  - 散布角度：±20°
  - 发射间隔：0.15秒
  - 每次发射：8支箭

#### 箭矢运动学

**速度更新**：

每帧添加重力加速度：

$$\vec{v}_{new} = \vec{v}_{old} + \vec{g} \cdot \Delta t$$

其中重力加速度 $\vec{g} = (0, -9.8, 0)$ m/s²。

**位置更新**：

$$\vec{p}_{new} = \vec{p}_{old} + \vec{v}_{new} \cdot \Delta t$$

**朝向更新**：

箭矢朝向始终与速度方向一致，使用速度向量计算旋转：

$$\vec{forward} = normalize(\vec{v})$$

通过计算 $\vec{forward}$ 与初始前向向量 $(1,0,0)$ 的旋转矩阵来更新模型变换。

**发射时的随机扰动**：

为模拟真实的箭矢散布效果，初速度添加随机扰动：

$$\vec{v}_{init} = \vec{v}_{base} + (\text{rand}(-1,1), \text{rand}(-0.5,0.5), \text{rand}(-1,1)) \cdot k_{spread}$$

$k_{spread} = 0.3$ 是散布系数。

### 5.5 碰撞检测系统

#### AABB包围盒原理

**AABB (Axis-Aligned Bounding Box)** 是与坐标轴对齐的矩形包围盒，用最小点和最大点表示：

$$AABB = [\vec{P}_{min}, \vec{P}_{max}]$$

其中：
$$\vec{P}_{min} = (x_{min}, y_{min}, z_{min})$$
$$\vec{P}_{max} = (x_{max}, y_{max}, z_{max})$$

#### 点-AABB相交检测

判断点 $\vec{P}$ 是否在AABB内：

$$x_{min} \leq P_x \leq x_{max} \land y_{min} \leq P_y \leq y_{max} \land z_{min} \leq P_z \leq z_{max}$$

#### 箭矢碰撞检测

箭矢使用其当前位置作为点，检测与以下物体的碰撞：

**1. 房间边界**

六个面的AABB：
- 地面：$Y = Y_{floor}$
- 天花板：$Y = Y_{ceiling}$
- 左墙：$X = X_{left}$
- 右墙：$X = X_{right}$
- 前墙：$Z = Z_{front}$
- 后墙：$Z = Z_{back}$

**2. 桌子**

桌子的AABB根据其位置、尺寸计算：

$$AABB_{table} = [P_{table} - \frac{S_{table}}{2}, P_{table} + \frac{S_{table}}{2}]$$

**3. 书柜**

书柜的AABB会随着滑动动画实时更新：

$$P_{bookcase}(t) = P_{start} + (P_{end} - P_{start}) \cdot progress(t)$$

#### 碰撞响应

当检测到碰撞时：

1. **停止运动**：速度归零
   $$\vec{v} = (0, 0, 0)$$

2. **记录碰撞点**：保存碰撞位置，用于后续渲染

3. **保持朝向**：箭矢保持碰撞瞬间的朝向，模拟"插在表面"的效果

4. **生命周期**：碰撞后箭矢保持可见，直到生命周期结束

## 六、镜头交互控制

### 6.1 相机系统

采用**第一人称自由相机**，支持键盘移动和鼠标视角控制。

### 6.2 键盘控制

| 按键 | 功能 | 方向 |
|------|------|------|
| W | 前进 | +前向量 |
| X | 后退 | -前向量 |
| A | 左移 | -右向量 |
| D | 右移 | +右向量 |

**注意**：后退使用X键而非S键，是为了避免与其他常用快捷键冲突。

移动公式：

$$\vec{P}_{new} = \vec{P}_{old} + \vec{D}_{move} \cdot speed \cdot \Delta t$$

其中移动方向 $\vec{D}_{move}$ 根据按键确定，$speed = 2.5$ 单位/秒。

### 6.3 鼠标视角控制

使用**欧拉角**表示相机朝向：
- **Yaw（偏航角）**：绕Y轴旋转，控制水平朝向
- **Pitch（俯仰角）**：绕X轴旋转，控制垂直朝向

#### 鼠标输入转换为角度变化

$$\Delta yaw = \Delta x_{mouse} \cdot sensitivity$$
$$\Delta pitch = -\Delta y_{mouse} \cdot sensitivity$$

其中 $sensitivity = 0.1°$ 是鼠标灵敏度。

注意Y轴取负，因为屏幕Y轴向下，而俯仰角向上为正。

#### 俯仰角限制

为避免"万向锁"问题，限制俯仰角范围：

$$-89° \leq pitch \leq 89°$$

#### 前向向量计算

根据欧拉角计算相机的前向向量：

$$\vec{front} = \begin{pmatrix} 
\cos(pitch) \cdot \cos(yaw) \\
\sin(pitch) \\
\cos(pitch) \cdot \sin(yaw)
\end{pmatrix}$$

右向量和上向量通过叉乘计算：

$$\vec{right} = normalize(\vec{front} \times \vec{up}_{world})$$
$$\vec{up} = normalize(\vec{right} \times \vec{front})$$

### 6.4 视图矩阵

使用GLM的lookAt函数构造视图矩阵：

$$V = lookAt(\vec{eye}, \vec{eye} + \vec{front}, \vec{up})$$

这等价于：

$$V = \begin{bmatrix}
r_x & r_y & r_z & -\vec{r} \cdot \vec{eye} \\
u_x & u_y & u_z & -\vec{u} \cdot \vec{eye} \\
-f_x & -f_y & -f_z & \vec{f} \cdot \vec{eye} \\
0 & 0 & 0 & 1
\end{bmatrix}$$

其中 $\vec{r}$、$\vec{u}$、$\vec{f}$ 分别是右向量、上向量、前向量。

---